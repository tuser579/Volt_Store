import React from 'react';
import ErrorPage from '../components/ErrorPage'

const notFound = () => {
    return (
        <div>
            <ErrorPage />
        </div>
    );
};

export default notFound;